fix(server): rota /admin estável (app ou factory) + limpeza de lixo e cache-bust

- Remove linhas iniciadas por $ que quebravam o parser
- Se app = Flask(...): injeta rota /admin direta
- Se create_app(): cria blueprint backend/admin_bp.py e registra no factory
- Cache-bust em /static/admin.js no template
