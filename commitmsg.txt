fix(server): cabeçalho Flask refeito + imports corretos + rota /admin

- Remove lixo PowerShell do cabeçalho
- Garante imports: Flask, request, jsonify, abort, send_from_directory, Response, render_template
- Preserva conteúdo existente a partir de 'app = Flask('
- Cria rota /admin -> admin.html (se faltava)
- Cache-bust do /static/admin.js
