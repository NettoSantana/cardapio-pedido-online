fix(server): limpar linhas PowerShell do server.py e expor /admin

- Remove linhas com \/\System.Collections.Hashtable do Python
- Garante import render_template
- Cria rota /admin -> templates/admin.html (app = Flask(...))
- Cache-bust do /static/admin.js
